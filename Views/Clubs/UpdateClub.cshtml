@{
    ViewData["Title"] = "Update Club";
}

<h2 class="display-4">Update an Event!!</h2>
<div class="row">
    <div class="col-12 justify-content-center">
        <section id="CreateForm">
            @using (Html.BeginForm("Update", "Clubs", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
            {
                <hr />
                <div class="form-group">
                    @Html.Label("name", "Enter the Club Id: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("id", "", new { @class = "form-control form-control-lg",@type= "number"  })
                    </div>
                </div>
                <div class="form-group">
                    @Html.Label("name", "Enter the Club name: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("name", "", new { @class = "form-control form-control-lg"})
                    </div>
                </div>
                <div class="form-group">
                    @Html.Label("name", "Enter the club description: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("description", "", new { @class = "form-control form-control-lg" })

                    </div>
                </div>
                <div class="form-group">
                    @Html.Label("name", "Enter the club creation year: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("creationYear", "", new { @class = "form-control form-control-lg",@type= "number"  })
                    </div>
                </div>

                <div class="form-group">
                    @Html.Label("name", "Enter the club website: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("website", "", new { @class = "form-control form-control-lg" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.Label("name", "Enter the club logo: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("logo", "", new { @class = "form-control form-control-lg" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.Label("name", "Enter the club president: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("president", "", new { @class = "form-control form-control-lg" })
                    </div>
                </div>
                <div class="form-group">
                    @Html.Label("name", "Enter the club HR: ", new { @class = "control-label lead" })
                    <div class="col-md-10">
                        @Html.TextBox("hr", "", new { @class = "form-control form-control-lg" })
                    </div>
                </div>
                <div class="form-group">
                    <div class="col-md-offset-2 col-md-10">
                        <input type="submit" value="Update" class="btn btn-danger btn-lg" />
                    </div>
                </div>
            }
        </section>
    </div>
</div>
